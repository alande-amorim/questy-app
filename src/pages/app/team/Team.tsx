import AppLayout from "../../../components/layouts/AppLayout/AppLayout";

export default function Team(props: { disableCustomTheme?: boolean }) {
  return <AppLayout pageTitle="Team">Team</AppLayout>;
}
