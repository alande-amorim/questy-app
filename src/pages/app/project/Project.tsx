import AppLayout from "../../../components/layouts/AppLayout/AppLayout";

export default function Project(props: { disableCustomTheme?: boolean }) {
  return <AppLayout pageTitle="Project">Project</AppLayout>;
}
